name: pdf

on:
  push:
    branches:
      - main
jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v5

      - name: Build LaTex
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex
